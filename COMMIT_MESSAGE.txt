Security Fixes and UI Cleanup (June 30, 2025)

This commit addresses critical security vulnerabilities and cleans up redundant UI elements:

1. Security Improvements:
   - Fixed JWT logout vulnerability by implementing token blacklisting using KV storage
   - Added unique token IDs (jti) to JWT tokens for proper invalidation
   - Updated auth middleware to check for blacklisted tokens
   - Improved menu renderer to handle multiple API endpoints gracefully

2. Admin UI Cleanup:
   - Removed duplicate import button generation in setupImportHandlers()
   - Fixed ID conflicts between HTML-defined and JavaScript-generated elements
   - Corrected status element references to match actual HTML structure
   - Cleaned up redundant code that was creating extra import sections

3. Bug Fixes:
   - Fixed menu renderer to try multiple API endpoints for better reliability
   - Improved error handling in menu loading with proper fallbacks
   - Enhanced price formatting to ensure consistent display

4. Code Quality:
   - Removed redundant import UI generation
   - Fixed syntax errors from incomplete function cleanup
   - Improved console logging for better debugging

These changes maintain all existing functionality while significantly improving security and reducing UI confusion caused by duplicate elements.

Related files:
- src/handlers/auth.ts (JWT security improvements)
- src/middleware/auth.ts (token blacklist checking)
- public/jss/admin-unified.js (import UI cleanup)
- public/menu/menu-renderer.js (improved reliability)
